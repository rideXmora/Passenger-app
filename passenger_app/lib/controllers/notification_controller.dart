import 'package:passenger_app/api/utils.dart';
import 'package:passenger_app/modals/organization.dart';
import 'package:get/get.dart';

class NotificationController extends GetxController {
//   Future<List<Organization>> subscribeToGetRequests({required String token}) async {
//     String url = '/api/common/allOrg';
//     dynamic response = await profileComplete(
//           name: name,
//           email: email,
//           city: city,
//           driverOrganization: {
//             "id": driverOrganization.id,
//             "name": driverOrganization.name,
//           },
//           token: Get.find<UserController>().driver.value.token);
//     return response;
//   }
// unsubscribeNotToGetRequests
//   List<Organization> unsubscribeNotToGetRequests(Map<dynamic, dynamic> response) {
//     List<Organization> orgList = [];
//     if (response["error"] != true) {
//       for (var item in response["body"]) {
//         Organization org = Organization(id: item["id"], name: item["name"]);
//         orgList.add(org);
//       }
//     }

//     return orgList;
//   }
}
